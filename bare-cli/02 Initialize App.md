###### Create React native CLI App
```
npx @react-native-community/cli init
```
```sh 
npm start
```
```sh
npx run android
```

if `npx react-native run-android` stuck then,
```sh
cd android
./gradlew clean
cd ..
npx react-native run-android
```

start app
```sh
npx react-native start
```